/**
 * Norwegian crossword clues: a short DEFINITION in Norwegian (`de` field) + its
 * English equivalent (`en`) — NOT the literal answer. Keyed by lemma id.
 * Authored for the early-block crossword words; later words fall back to the
 * plain English gloss until clues are added.
 */
import type { Clue } from '../../clues';

export const CLUES_NO: Record<string, Clue> = {
};
